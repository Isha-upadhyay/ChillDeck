def generate_slide_content(slide_title: str, research_points: list):
    """
    Returns bullet points for a slide using research points + AI generation
    """
    bullets = []
    for point in research_points:
        # Placeholder: dynamic text generation
        bullets.append(f"{point} - explained dynamically")
    return bullets
